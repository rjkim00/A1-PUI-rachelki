//Typing Title Animation effect
var options = {
    strings: ['RELIVED', 'LOVED', 'CHERISHED', 'PRESERVED'],
    typeSpeed: 100,
    backSpeed: 100,
    loop: true
};

var typed = new Typed('.typing', options);